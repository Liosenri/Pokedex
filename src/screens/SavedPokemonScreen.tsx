import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const SavedPokemonScreen = (props: Props) => {
  return (
    <View>
      <Text>SavedPokemon</Text>
    </View>
  );
};

export default SavedPokemonScreen;

const styles = StyleSheet.create({});
